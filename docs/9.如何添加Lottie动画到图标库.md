# 如何添加Lottie动画到图标库

## 📋 概述

IconPicker组件已集成Lottie动画支持，用户可以在配置名片模块时选择动画图标。

**当前可用动画**：
- 电话响铃 (`phone-ring`)
- 分享浮动 (`share-float`)

---

## 🚀 方法一：使用快速添加脚本（推荐）

### 1. 准备动画文件

从以下网站下载免费Lottie动画：
- **LottieFiles**: https://lottiefiles.com/ (最大的动画库)
- **Iconscout**: https://iconscout.com/lottie-animations
- **Lordicon**: https://lordicon.com/

下载格式：**JSON格式**（Lottie JSON）

### 2. 运行添加脚本

```bash
cd /opt/qwcard
./scripts/add-lottie-animation.sh <动画文件> <中文名称> <类别> [子目录]
```

**示例**：
```bash
# 添加点赞动画
./scripts/add-lottie-animation.sh ~/Downloads/like.json 点赞动画 interaction

# 添加加载动画
./scripts/add-lottie-animation.sh loading.json 加载中 status loading

# 添加成功动画
./scripts/add-lottie-animation.sh success.json 成功提示 status status
```

**类别选项**：
- `contact` - 联系相关（电话、邮件等）
- `action` - 操作动作（分享、下载等）
- `business` - 商业相关（支付、交易等）
- `interaction` - 交互动画（点击、滑动等）
- `status` - 状态指示（成功、失败、加载等）

### 3. 手动更新配置

脚本会提示你需要添加的配置代码，复制并粘贴到 `/opt/qwcard/config/icon-library.js` 中：

```javascript
export const LOTTIE_ANIMATIONS = [
  // 现有动画...
  {
    key: 'like',
    name: '点赞动画',
    path: 'interaction/like.json',
    category: 'interaction',
    preview: '/assets/animations/interaction/like.json',
    description: '点赞互动效果'
  }
]
```

### 4. 重启前端服务

```bash
systemctl restart wecard-nuxt.service
```

---

## 🛠️ 方法二：完全手动添加

### 步骤1：上传动画文件

将JSON文件上传到服务器：

```bash
# 创建子目录（如果不存在）
mkdir -p /opt/qwcard/public/assets/animations/interaction

# 上传文件
scp like.json root@your-server:/opt/qwcard/public/assets/animations/interaction/
```

或使用SFTP工具（如FileZilla）上传到：
```
/opt/qwcard/public/assets/animations/interaction/
```

### 步骤2：编辑配置文件

编辑 `/opt/qwcard/config/icon-library.js`：

```javascript
export const LOTTIE_ANIMATIONS = [
  {
    key: 'phone-ring',
    name: '电话响铃',
    path: 'phone/ring.json',
    category: 'contact',
    preview: '/assets/animations/phone/ring.json',
    description: '电话拨打动画效果'
  },
  {
    key: 'share-float',
    name: '分享浮动',
    path: 'share/float.json',
    category: 'action',
    preview: '/assets/animations/share/float.json',
    description: '分享按钮浮动效果'
  },
  // ✅ 添加你的新动画
  {
    key: 'like',              // 唯一标识（kebab-case）
    name: '点赞动画',          // 显示名称
    path: 'interaction/like.json',  // 相对路径
    category: 'interaction',  // 类别
    preview: '/assets/animations/interaction/like.json',  // 预览路径
    description: '点赞互动效果'  // 可选描述
  }
]
```

### 步骤3：重启服务

```bash
systemctl restart wecard-nuxt.service
```

### 步骤4：验证

等待30秒后，打开租户工作区：
1. 添加任意带图标的模块（如"产品服务"）
2. 点击图标选择器
3. 切换到"Lottie动画"标签
4. 应该能看到新添加的动画

---

## 📁 推荐的目录结构

```
/opt/qwcard/public/assets/animations/
├── contact/          # 联系相关
│   ├── phone-ring.json
│   ├── email-send.json
│   └── wechat-scan.json
├── action/           # 操作动作
│   ├── share-float.json
│   ├── download.json
│   └── upload.json
├── interaction/      # 交互动画
│   ├── like.json
│   ├── click.json
│   └── swipe.json
├── status/           # 状态指示
│   ├── loading.json
│   ├── success.json
│   └── error.json
└── business/         # 商业相关
    ├── payment.json
    └── deal.json
```

---

## ⚠️ 注意事项

### 文件大小
- **推荐**：< 50KB
- **最大**：< 200KB
- 过大的文件会影响加载速度

### 文件命名
- 使用英文、数字、连字符
- 避免空格和特殊字符
- 示例：`like.json`、`phone-ring.json`、`success-check.json`

### 性能优化
1. 优先选择简单的动画
2. 避免过于复杂的路径
3. 使用PNG降级方案（如需要）

### 兼容性
- IconPicker会自动预加载所有Lottie动画
- 加载失败时会显示图标名称
- 支持循环播放和自动播放

---

## 🔍 测试新动画

### 1. 检查文件是否存在
```bash
ls -lh /opt/qwcard/public/assets/animations/interaction/like.json
```

### 2. 验证JSON格式
```bash
cat /opt/qwcard/public/assets/animations/interaction/like.json | jq '.' > /dev/null && echo "✅ JSON格式正确" || echo "❌ JSON格式错误"
```

### 3. 在浏览器测试
访问：`https://你的域名/assets/animations/interaction/like.json`  
应该能看到JSON内容（不要看到404）

### 4. 查看前端日志
```bash
journalctl -u wecard-nuxt -f
```
检查是否有加载错误

---

## 🎨 推荐动画资源

### 免费资源
1. **LottieFiles** (https://lottiefiles.com/)
   - 最大的Lottie社区
   - 100,000+ 免费动画
   - 支持在线编辑

2. **Iconscout** (https://iconscout.com/lottie-animations)
   - 高质量动画
   - 部分免费

3. **Lordicon** (https://lordicon.com/)
   - 专业图标动画
   - 免费套餐可用

### 常用动画类型
- 联系方式：电话、邮件、位置、微信
- 社交互动：点赞、评论、分享、关注
- 状态提示：加载、成功、失败、警告
- 操作反馈：点击、滑动、拖拽、下载
- 商业场景：支付、交易、数据、图表

---

## 📞 问题排查

### 动画不显示
1. 检查文件路径是否正确
2. 验证JSON格式是否有效
3. 查看浏览器控制台错误
4. 确认前端服务已重启

### 动画加载慢
1. 检查文件大小（建议<50KB）
2. 优化动画复杂度
3. 考虑使用CDN

### 动画不流畅
1. 降低动画帧率
2. 简化路径和图层
3. 使用LottieFiles编辑器优化

---

## 📝 配置字段说明

```javascript
{
  key: 'like',                    // 必填，唯一标识，用于内部引用
  name: '点赞动画',                // 必填，显示名称，用户可见
  path: 'interaction/like.json',  // 必填，相对animations/目录的路径
  category: 'interaction',        // 必填，分类（便于管理）
  preview: '/assets/animations/interaction/like.json',  // 必填，完整预览路径
  description: '点赞互动效果'      // 可选，动画描述
}
```

---

**更新时间**：2025-10-13  
**相关文档**：`/opt/qwcard/docs/8.Lottie简单配置指南.md`

